// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// generator client {
//   provider = "prisma-client-js"
// }
// 
// datasource db {
//   provider = "mongodb"
//   url      = env("DATABASE_URL")
// }

datasource db {
  provider     = "mysql"
  url          = env("mysql://99kdsck92616bkwzm2ni:pscale_pw_gEqtImLAB0b3UQgcfXDRXxWWGCr5qDpzmBsTaZDPkNX@aws.connect.psdb.cloud/hotel-companion?sslaccept=strict")
  relationMode = "prisma"
}

generator client {
  provider = "prisma-client-js"
}


model Event {
  id                 Int   @id @default(autoincrement())
  eventType          String
  eventName          String
  eventDate          DateTime
  startTime          String
  endTime            String
  venue              String
  additionalServices String
  valetService       Boolean
  valetPrice         Float?
  guestCount         Int?
  roomCount          Int?
  organizer          String? 
  description        String
}

model Todo {
  id          Int      @id @default(autoincrement())
  createdAt   DateTime @default(now())
  guestName   String
  roomNumber  String
  title       String
  description String
  completed   Boolean  @default(false)
  createdBy   String
}

model MessageTemplate {
  id          Int      @id @default(autoincrement())
  title       String
  description String
}